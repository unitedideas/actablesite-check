name: Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20.x, 22.x]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
      - run: npm test
      - run: npm pack --dry-run
      - name: Run the local GitHub Action
        id: crawler-policy
        uses: ./
        with:
          website: example.com
          fail-on-blocked: "false"
          check-edge-responses: "true"
          fail-on-restricted: "false"
      - name: Verify action outputs
        env:
          ALLOWED_COUNT: ${{ steps.crawler-policy.outputs.allowed-count }}
          BLOCKED_COUNT: ${{ steps.crawler-policy.outputs.blocked-count }}
          RESTRICTED_COUNT: ${{ steps.crawler-policy.outputs.restricted-count }}
        run: test "$((ALLOWED_COUNT + BLOCKED_COUNT))" -eq 8 && test "$RESTRICTED_COUNT" -ge 0
